import React from "react";

export default function Floor({ polygon }) {
    return <polygon
        points={polygon}
        fill="#ffffaa"
        stroke="steelblue"
        strokeWidth={8}
    />;
}
///add memo
